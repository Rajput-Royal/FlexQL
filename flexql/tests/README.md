# Tests

Place `benchmark_flexql.cpp` from the course repository here:
https://github.com/Bivas-Biswas/FlexQL_Benchmark_Unit_Tests

Then build with:
    make benchmark

And run with:
    ./bin/benchmark 127.0.0.1 9000
